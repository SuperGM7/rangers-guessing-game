import React from 'react';
import ReactDOM from 'react-dom/client';
import RangersGuessingGame from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RangersGuessingGame />
  </React.StrictMode>
);
